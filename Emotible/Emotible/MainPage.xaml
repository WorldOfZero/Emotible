<Page
    x:Class="Emotible.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Emotible"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:Emotible.ViewModels"
    mc:Ignorable="d">
    
    <Page.Resources>
        <viewmodels:EmoteListViewModel x:Key="Emotes" x:Name="Emotes"></viewmodels:EmoteListViewModel>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ItemsControl DataContext="{StaticResource Emotes}" ItemsSource="{Binding EmotesList}"
                      Margin="12">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding Content}"
                            HorizontalAlignment="Stretch"
                            Command="{Binding OnClick}"
                            CommandParameter="{Binding Content}"></Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>
